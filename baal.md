# "לאן נטייל?" // מערכת חכמה להמלצות טיולים מותאמות מזג אוויר ושטח — מסמך אפיון, ארכיטקטורה, מודל עלויות ומצגת מנהלים

---

## 1. תקציר מנהלים (Executive Summary)

**"לאן נטייל?" (Travel Weather Recommender)** היא פלטפורמה חכמה וחווייתית להמלצה והתאמת מסלולי טיול, שמורות טבע, גנים לאומיים ובתי ספר שדה בישראל — **בהתאמה מלאה למזג האוויר ולתנאי השטח בזמן אמת**.

המערכת משלבת **מנוע המלצות חכם (Agent BAAL)** המבצע היתוך מידע בין נתוני מזג אוויר מקומיים (עומסי חום, משקעים, רוחות) לבין מאפייני האתרים, וממליץ למשפחות ומטיילים על היעדים האידיאליים ביותר לכל יום (למשל: מעיינות ומערות מוצלות בימי שרב, או שמורות טבע צפוניות וגנים ארכיאולוגיים יבשים בימים גשומים).

המערכת פועלת על פי **מדיניות קשיחה של 0$ עלות תפעולית** עם זמן תגובה תת-שנייתי (<800ms), תוך שימוש במודלים מקומיים, מפות פתוחות ומבנה נתונים מקומי ומאובטח.

---

## 2. חזון ומטרות הפרויקט (Vision & Core Objectives)

| מטרה | פירוט |
| :--- | :--- |
| **המלצות טיול מותאמות מזג אוויר** | התאמה חכמה בין מזג האוויר החזוי (שרב, גשם, מזג אוויר נעים) למסלול הטיול המושלם. |
| **בטיחות מונעת ומצילת חיים** | מניעת כניסה יזומה לאזורי סיכון (שיטפונות בנחלי ים המלח, עומס חום קיצוני במדבר) והפניה חלופית לאתרים בטוחים. |
| **חוויית משתמש יפהפייה (OutletBuddy Dribbble UX)** | ממשק מודרני, נקי, ללא עומס קוגניטיבי, כרטיס מידע צף על גבי המפה, וזמן תגובה מיידי (פחות משנייה). |
| **עצמאות טכנולוגית ועלות אפס (Zero-Cost)** | ריצה מקומית מלאה ללא תלות במנויי ענן יקרים, ללא מפתחות API בתשלום, ושרידות מלאה. |

---

## 3. ארכיטקטורת המערכת (System Architecture)

```mermaid
graph TD
    subgraph Client_SPA [Frontend - React 18 + Vite + Tailwind CSS]
        UI_Sidebar[Left Sidebar - 30%\nSearch, Filters, Site Cards List]
        UI_Map[Main Map - 70%\nLeaflet GIS + Badge Pins + Route Vector]
        UI_Modal[Floating Map Detail Card\nMetric Pills, XAI Summary, Action Btn]
        UI_Footer[Status & Telemetry Ticker\n[SYSTEM] | [HARVEST] | [AGENT]]
    end

    subgraph Agentic_Core [The Brain - Agent BAAL]
        AgentService[AgentService.js\nRequest Orchestrator & Timeout Controller]
        Local_LLM[Local LLM - Ollama / Llama 3\nhttp://127.0.0.1:11434/api/generate]
        Heuristic_Failsafe[Deterministic Rule Heuristic\nInstant $0 Offline Failover]
    end

    subgraph Data_Layer [Data & Skills Layer]
        Assets_DB[(assets_db.json\n19 National Sites & Safe Havens)]
        Skill_Weather[fetch_weather_osint.py\nPrecipitation, Heat Index, Storms]
        Skill_Risk[evaluate_asset_risk.py\nVulnerability Matrix Cross-Reference]
        Skill_Haven[find_safe_alternative.py\nNearest Safe Haven Optimization]
    end

    UI_Sidebar --> AgentService
    AgentService -->|Vite Proxy /api/chat| Local_LLM
    Local_LLM -.->|Fallback on error/offline| Heuristic_Failsafe
    AgentService --> Data_Layer
    AgentService -->|JSON Decision & Vector| UI_Map
    AgentService -->|Updated Cards Matrix| UI_Sidebar
    AgentService -->|Floating Card Data| UI_Modal
```

### פירוט שכבות המערכת:

1. **שכבת התצוגה (Frontend Layer)**:
   - **טכנולוגיות**: React 18, Vite 6, Tailwind CSS (v3.4), Leaflet GIS, Lucide Icons.
   - **עיצוב (Shahar Design System)**: Flat Dark Slate/Zinc (`#121214`), גופן `Heebo` / `Inter`, תגיות מעוצבות, נתיב ניתוב ירוק-מנטה (`#34d399`), וכרטיס מידע צף (Floating Map Card).
   - **רספונסיביות**: חלוקת מסך 30% סרגל צד + 70% מפה אינטראקטיבית.

2. **שכבת הסוכן (Autonomous Agent BAAL)**:
   - **הסקה מקומית**: תקשורת ישירה מול שרת Ollama מקומי (מודל `Llama 3`) באמצעות פרוקסי Vite (`/api/chat` -> `127.0.0.1:11434/api/generate`).
   - **זמן תגובה מוגבל (Timeout)**: בקרת שגיאות קשיחה עם Timeout של 10 שניות.
   - **מנגנון גיבוי אוטונומי (Failsafe)**: מודל היוריסטי מבוסס חוקים דטרמיניסטיים הפועל ב-100% שרידות גם ללא חיבור ל-LLM או בהיעדר רשת.

3. **שכבת הנתונים והכלים (Skills & Database)**:
   - **`assets_db.json`**: מסד נתונים מובנה הכולל 19 אתרים ארציים:
     - גנים לאומיים ושמורות טבע (מצדה, עין גדי, תל דן, קיסריה וכו').
     - כל 13+ בתי ספר שדה של החברה להגנת הטבע (חרמון, גולן, מירון, אכזיב, כרמל, עין גדי, הר הנגב, אילת וכו').
     - מקלטים בטוחים מבוצרים (Safe Havens): בית גוברין (105), ציפורי (106), כפר עציון (211), תחנת הצפרות ירושלים (212).
   - **סקילים טקטיים (Antigravity Skills)**: מודולי Python מאומתים ב-`tests/test_skills.py` להערכת סיכונים, שליפת נתוני מזג אוויר וחישוב מרחקי מילוט. לניתוח המתמטי והארכיטקטוני המלא ראה: [skills_analysis.md](file:///c:/Projects/Tyul/skills_analysis.md).

---

## 4. מודל עלויות: התחייבות ל-0$ (Zero-Cost Economics)

המערכת פותחה תחת כלל ברזל של **0$ עלות הקמה ו-0$ עלות תפעול שוטפת**:

| רכיב תשתית | פתרון מסחרי מקובל (עלויות חודשיות) | פתרון GeoGuard ($0 Cost) | חיסכון חודשי מוערך |
| :--- | :--- | :--- | :--- |
| **הסקה ומודלי שפה (LLM)** | OpenAI GPT-4o / Claude API ($0.03-$0.06 ל-1K tokens) | **Ollama / Llama 3 מקומי** על המחשב (`127.0.0.1:11434`) + היוריסטיקת Failsafe | **$50 - $300** |
| **שרתי מפות ו-GIS** | Mapbox / Google Maps Platform API ($5-$7 ל-1,000 טעינות מפה) | **שכבות חינמיות ללא סימני מים** (Google Free Tile Server / CartoDB / OSM) | **$100 - $500** |
| **נתוני מזג אוויר ו-OSINT** | API מסחרי של AccuWeather / OpenWeather Pro ($40-$150/חודש) | **סימולציות מקומיות דטרמיניסטיות + Open-Meteo Free Tier** (ללא כרטיס אשראי) | **$40 - $150** |
| **אחסון ענן ושרתי Backend** | AWS ECS / GCP Cloud Run / Supabase ($30-$80/חודש) | **ריצה מקומית מלאה (Client-Side Vite SPA + Local Python)** | **$30 - $80** |
| **סה"כ עלות חודשית** | **$220 - $1,030+ לחודש** | **$0.00 לחודש (חינם לחלוטין)** | **100% חיסכון** |

---

## 5. ניתוח סיכונים ואבטחת מידע (Security & Vulnerabilities Analysis)

### א. סיכוני שטח וסביבה (Operational Environmental Threats)
המערכת ממדלת ומנטרת 4 וקטורי סיכון מרכזיים:
1. **שיטפונות בזק (Flash Floods)**: ניטור אגני ניקוז במדבר יהודה, ים המלח והנגב. אתרים פגיעים: עין גדי, מצדה, עין עבדת.
2. **עומסי חום קיצוניים (Extreme Heat)**: ניטור טמפרטורה מעל 42°C באזורים מדבריים ונמוכים.
3. **סערות ים ורוחות חזקות (Gale Winds & Marine Storms)**: ניטור חופי קיסריה, אכזיב ומעגן מיכאל.
4. **שלג, קרה וקיפאון (Freezing / Heavy Snow)**: ניטור צפון הגולן, הר מירון והחרמון.

### ב. אבטחת מידע, סייבר ופרטיות (Cybersecurity & Data Privacy Posture)
- **Zero-Cloud PII Leaks (פרטיות מלאה)**: שום מידע על מיקום המטיילים, מסלולים מתוכננים או הרגלי תנועה אינו נשלח לשרתי ענן צד-שלישי. כל העיבוד מתבצע מקומית במכשיר/דפדפן.
- **Air-Gapped Ready**: המערכת מסוגלת לפעול באופן מלא בסביבה מנותקת רשת (Offline / Emergency Bunker Mode) באמצעות ה-Failsafe ההיוריסטי המובנה ומסד הנתונים הלוקאלי.
- **הגנה מהזרקות פרומפטים (Prompt Injection Defense)**: פלט ה-LLM נאכף למבנה JSON קשיח בלבד ומאומת ע"י Schema Validator לפני עדכון ה-State של המפה.
- **איטום שגיאות (Graceful Degradation)**: במקרה של קריסת שרת ה-LLM, המערכת אינה קופאת או מציגה שגיאות קוד, אלא מתריעה ביומן ההחלטות ומפעילה מנגנון Failsafe אוטונומי בתוך פחות מ-800 מילישניות.

---

## 6. תוכנית שקפים למצגת מנהלים (Executive PPT Blueprint)

להלן מבנה מלא מומלץ למצגת בת 8 שקפים:

```
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 1: שער המצגת                              │
│  כותרת: GeoGuard // לאן נטייל?                                         │
│  כותרת משנה: חמ"ל בטיחות סיורים וטיולים מונחה סוכן בינה מלאכותית       │
│  תגיות: Autonomous C4I • Zero-Cost ($0) • Family Mission Control        │
└────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 2: הבעיה והצורך                           │
│  • טיולים בשטחי ישראל חשופים לשינויי מזג אוויר מהירים (שיטפונות, חום). │
│  • התרעות מזג אוויר כלליות אינן מותאמות למיקום המטייל בזמן אמת.        │
│  • לוחות מחוונים קיימים הם פסיביים ודורשים מהמשתמש לפענח מידע מורכב.   │
└────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 3: הפתרון - סוכן BAAL                     │
│  • סוכן טקטי אוטונומי המבצע היתוך מידע בזמן אמת (OSINT + Weather).     │
│  • זיהוי אוטומטי של התאמת רגישות האתר לאיום הסביבתי.                   │
│  • הפעלה מיידית של פרוטוקול Safe Haven: הפניה אוטונומית למקלט בטוח.  │
│  • הסברתיות מלאה (XAI) לכל החלטה עם ציון ביטחון (Confidence Score).    │
└────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 4: ארכיטקטורת המערכת                      │
│  • Frontend: React 18 + Vite + Tailwind CSS (עיצוב Dribbble מודרני).   │
│  • AI Core: Local Ollama (Llama 3) דרך Vite Proxy ללא תלות בענן.       │
│  • Data: מסד נתונים של 19 אתרים (רט"ג, החברה להגנת הטבע, מקלטים).    │
│  • Resilience: מנגנון Failsafe היוריסטי מקומי לשרידות 100% Offline.   │
└────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 5: חוויית המשתמש והעיצוב                 │
│  • סרגל חיפוש וסינון חכם (קרבה, סוג שמורה, ציוני בטיחות).              │
│  • מפה טקטית עם פינים מעוצבים וציוני בטיחות מוצמדים (Badge Pins).       │
│  • כרטיס מידע צף אינטראקטיבי (Floating Map Detail Card).               │
│  • נתיבי ניתוב ירוקים מוארים (Mint Green Vectors) בעת התראה.           │
└────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 6: כלכלת הפרויקט (0$ Cost)                │
│  • AI: 0$ (Local LLM Llama 3 במקום מאות דולרים ב-OpenAI API).          │
│  • Maps: 0$ (שכבות מפה חינמיות בעברית ללא דמי שימוש).                  │
│  • Hosting: 0$ (ריצה לוקאלית מלאה בדפדפן הלקוח).                       │
│  • ROI: חיסכון של 100% בעלויות תפעול שוטפות.                           │
└────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 7: אבטחה, שרידות ופרטיות                  │
│  • פרטיות מוחלטת: אין שידור של נתוני מיקום לעננים מסחריים.             │
│  • פעולה במצב מנותק (Offline Bunker Mode).                             │
│  • הגנה מפני הזרקת פרומפטים ואימות קלט/פלט קשיח.                       │
└────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        SLIDE 8: הדגמת חמ"ל חי וסיכום                  │
│  • הדגמה חיה: סימולציית שיטפון בים המלח -> ניתוב אוטונומי לבית גוברין. │
│  • סטטוס נוכחי: מערכת פעילה ומאומתת עם בדיקות יחידה מלאות.            │
│  • שלב הבא: התממשקות ל-IoT חיישני שטח וחיבור למכשירי קשר לווייניים.   │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 7. מדריך הפעלה מהיר (Runbook)

```bash
# 1. הפעלת מודל ה-LLM המקומי (אופציונלי, קיים Failsafe אוטונומי מלא)
ollama run llama3

# 2. הרצת בדיקות היחידה של הסקילים (Python Skills Test Suite)
pytest tests/test_skills.py

# 3. הפעלת שרת הפיתוח של הממשק
npm run dev
# פתח את הדפדפן בכתובת: http://localhost:5173/

# 4. ביצוע Build לייצור (Production)
npm run build
```

---

*מסמך זה נערך ונבדק בהתאם להנחיות הארכיטקטורה של סוכן BAAL וסוכן המעצב שחר (Lead Product Designer).*
